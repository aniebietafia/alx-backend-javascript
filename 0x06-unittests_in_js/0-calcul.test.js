const assert = require('assert');

describe('rounded', () => {
  describe('#Math.round()', () => {
    it('should round a and b and return the sum of it', () => {
      assert.equal(Math.round(a) + Math.round(b), 'number');
    });
  });
});
